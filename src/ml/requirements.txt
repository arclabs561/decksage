# NOTE: pyproject.toml is the canonical source for dependencies
# This file is kept for:
# 1. Path detection markers (used by path_setup.py, paths.py)
# 2. Legacy scripts that may reference it
# For new projects, use: uv sync (reads pyproject.toml)

# Core dependencies
pecanpy>=2.0.0
gensim>=4.3.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
pyyaml>=6.0
scipy>=1.10.0
python-dotenv>=1.0.0
requests>=2.31.0

# API
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
pydantic-ai>=0.0.14

# Optional (for advanced features)
umap-learn>=0.5.4
plotly>=5.17.0
