#!/usr/bin/env python3
"""Former script-like test moved out of pytest discovery.

Run manually if needed: uv run python test_enrichment_optimizations.py
"""

if __name__ == "__main__":
    # Keep as a no-op to avoid pytest collection; real checks live in unit tests.
    print("This helper script is not part of pytest. Run it manually if desired.")
